{
    "name": "controle-gastos-backend",
    "version": 2,
    "public": true,
    "builds": [{
        "src": "bin/server.js",
        "use": "@vercel/node"
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "bin/server.js"
        }
    ]
}