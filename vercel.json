{
    "name": "controle-gastos-backend",
    "version": 1,
    "public": true,
    "builds": [{
        "src": "bin/server.js",
        "use": "@vercel/node"
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "bin/server.js"
        }
    ]
}